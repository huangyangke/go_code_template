bin/
logs/
*.log

# 编译产物（裸二进制，应放 bin/ 或临时构建）
/server
/migrate

# SQLite 数据库文件（本地开发产出，不提交）
data/

# 环境变量文件含真实密码，不提交
# .env.dev / .env.prod 是模板，提交时只保留占位值
*.env.local

# Swagger 生成产物，由 build 自动生成，不提交
docs/
