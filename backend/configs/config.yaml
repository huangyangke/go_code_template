app:
  family: go-template
  host: 0.0.0.0
  port: 8080

log:
  level: info
  dir: logs

mysql:
  dsn: ${MYSQL_DSN}
  max_open_conns: 20
  max_idle_conns: 5

redis:
  addrs:
    - ${REDIS_ADDR:127.0.0.1:6379}
  password: ${REDIS_PASSWORD:}
  db: 0
  type: standalone
